this folder contains some self-contained scripts that we built to serve as starting points for other components in the system

these may not be runnable on their own, they are just reminders for last known functional versions of some components before they integrated into the full system